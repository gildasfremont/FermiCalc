// Initialize translations object
const translations = {
    // ... all translations stay exactly the same ...
};

// Trigger the translations loaded event
window.dispatchEvent(new Event('translationsLoaded'));
